#pragma once

/*
   Define: NPE_USE_THREADS
   
   Build flag that when defined states that the system should be build with multi-threading support.

   Enabling this requires additional dependencies depending on your compiler and platform.
   GCC/Linux: Build and link with -pthread
*/
